## 1. View mode state

- [x] 1.1 In `src/store/uiSlice.ts`, add `viewMode: 'editor' | 'preview'` to `UiState`, default `'editor'` in `createInitialUiState()`, and add a `setViewMode: (mode: UiState['viewMode']) => void` action following the same shape as `setLayoutMode`/`setActiveTool` (plain `set()`, no interaction with `activePageId`/`selectedElementIds`). **Deviation caught during task 5.1's regression test**: a plain `set()`-based `setViewMode` still pushed a redundant undo/redo history entry, because `zundo` pushes on every `set()` call regardless of whether the tracked `document` slice changed (no `equality`/`diff` configured) — the same issue `reanchorActivePageId` already works around. `store/index.ts` now overrides `createUiSlice`'s `setViewMode` with a version wrapped in `useEPPStore.temporal.getState().pause()`/`.resume()`. See design.md.
- [x] 1.2 Add `setViewMode` to the `UiSlice` interface and confirm it's exposed on `EPPStore` in `src/store/index.ts` the same way the other `ui` setters already are.

## 2. Shared, presentation-only image renderer

- [x] 2.1 Extract a small component (e.g. `src/components/canvas/SlotImage.tsx`) that takes an asset, a resolved box, scaling rule, specific size, rotation, and zoom, and renders exactly what `PageStage.tsx`'s inline slot-image block (~lines 330-402) renders today: the "missing image" fallback, or the positioned/rotated `<img>` for both the `specificSize` and default rendering paths — no click/hover/drag handlers, no selection border, no dimension overlay.
- [x] 2.2 Update `PageStage.tsx` to render `SlotImage` inside its existing slot wrapper `<div>` instead of the inline JSX, keeping all of that wrapper's interaction handlers, selection styling, and `DimensionOverlay` usage unchanged.
- [x] 2.3 Update `FreeformElementView` (`src/components/canvas/FreeformElement.tsx`) to render the same `SlotImage` for its image content (the `isSpecificSize`/default-`<img>` branch inside its `contentRef` wrapper), keeping its own move/resize/rotate handlers, selection chrome, and `DimensionOverlay` unchanged.
- [x] 2.4 Manually re-check the editor canvas after this extraction: assigned images, specific-size images (including the unsatisfied-size red outline), rotated images (90/270 included), and freeform elements all still render identically to before the refactor. Verified end-to-end under `xvfb` with Playwright's `_electron`: loaded two real images, dropped one onto the default slot (fitInParent renders correctly), rotated it to 270° (rotation renders correctly), switched to `specificSize` with an oversized width/height (locked dimension badge and clipping-by-overflow-hidden behavior match pre-refactor logic — the "unsatisfied" red outline is itself clipped out of view at this size, same as before the refactor, unrelated to it), and switched the root to `freeformCanvas` and dropped an image there (renders correctly, no handles).

## 3. Preview screen

- [x] 3.1 Add a `PreviewStage`-style component (e.g. `src/components/preview/PreviewStage.tsx`) that reads the active page via `useLayoutResolution()`, computes a fit-to-screen zoom against its own full-viewport container (same `computeFitZoom`/`clampZoom`/`ResizeObserver` pattern `PageStage.tsx` uses, no manual zoom controls exposed), and renders the page's white background at that scale.
- [x] 3.2 Within `PreviewStage`, render every `imageSlot` position from the resolved layout using `SlotImage` when assigned, and nothing (no placeholder, no border) when unassigned. Render every `freeformCanvas`'s elements the same way — position/scale/rotate via `SlotImage`, no selection handles, no empty-element placeholder. Do not render container outlines, dividers, or the root padding outline.
- [x] 3.3 Add a chrome-free page-navigation control for `PreviewStage` (e.g. `src/components/preview/PreviewPageSwitcher.tsx`, or reuse `PageSwitcher.tsx`'s prev/next logic without its Add/Remove buttons) showing "Page N of M" and previous/next controls wired to the existing `setActivePageId`, with the same no-wraparound behavior as the editor's switcher.
- [x] 3.4 Add the top bar for the preview screen with an "Export PDF" button, a "Print" button (both inert — no `onClick`, or a no-op handler), and an explicit exit control (e.g. "Back to editor") that calls `setViewMode('editor')`.

## 4. Wiring into the app shell

- [x] 4.1 In `src/App.tsx`, add the "Preview" button to the header, to the right of `<UnitToggle />`, calling `setViewMode('preview')`.
- [x] 4.2 In `src/App.tsx`, branch the root render on `ui.viewMode`: `'editor'` renders the existing layout unchanged, `'preview'` renders the new preview screen (header/aside/canvas/image-library replaced while active). **Deviation, caught by `/adversarial-review`**: the three `ConfirmDialog`s (New/Open project, missing images) are NOT replaced by the branch — they render as always-mounted siblings of both branches, since the menu listeners that open them (File > New, File > Open) stay active regardless of `viewMode`, and nesting the dialogs only inside the editor branch left them silently unshown while in preview. See design.md and the branch's fix commit.
- [x] 4.3 Update the existing `keydown` Escape handler in `src/App.tsx` to check `ui.viewMode` first: in `'preview'`, call `setViewMode('editor')` and return without calling `clearSelection()`; otherwise keep the current behavior unchanged.

## 5. Tests

- [x] 5.1 Unit test `setViewMode` / the `viewMode` default in `uiSlice.ts` (defaults to `'editor'`, toggles as expected, and is excluded from the store's tracked/undoable state the same way `layoutMode` already is verified to be, if such a test exists to extend). Added `src/store/uiSlice.test.ts` (default + toggle, other `ui` fields untouched) and `src/store/index.test.ts` (`setViewMode` describe block: toggling produces zero `pastStates` growth) — the latter caught the pause/resume gap noted in task 1.1.
- [x] 5.2 Unit test `SlotImage`'s rendering decisions if practical (missing-asset fallback vs. specific-size vs. default path), or, if the existing test suite covers this logic only through `PageStage`/`FreeformElement` integration tests, confirm those still pass unchanged after the extraction (task 2.4 covers the manual/visual side of this). Confirmed: the full suite (`src/lib/imageDisplay.test.ts`'s 28 tests over the underlying math, plus all `PageStage`/`FreeformElement`-adjacent tests) passes unchanged after the extraction — 141/141 total, no new `SlotImage`-specific unit test added since its three branches are already exhaustively covered indirectly and directly verified visually in task 2.4.

## 6. Verification

- [x] 6.1 Run the app end-to-end: click "Preview", confirm the editor's panels/gizmos disappear and the active page renders full-screen at fit-to-screen zoom, matching what the editor canvas shows content-wise (assigned images, empty slots blank, freeform elements without handles). Verified under `xvfb` with Playwright's `_electron`: a two-page document with an image assigned on page 1 and page 2 left blank — preview showed page 2 (the newly active page) as a plain white rectangle with no editor chrome, and page 1 with the assigned image positioned/scaled correctly, no borders/badges/overlays.
- [x] 6.2 Confirm the preview page switcher moves between pages without wraparound and without exiting preview, and that the exit control and Escape both return to the editor with the document/selection/layout mode unchanged. Verified: "Previous" moved page 2 → page 1 while staying in preview; "Previous" became disabled on page 1 (no wraparound); no Add/Remove Page controls present in preview; "Back to editor" and Escape (tried separately) both returned to the editor with the page indicator and the assigned `sample-a.jpg` still showing correctly.
- [x] 6.3 Confirm the "Export PDF" and "Print" buttons are visible and do nothing when clicked (no console error masking a real handler, no navigation, no dialog). Verified: clicked both, zero console errors/page errors captured, still in preview afterward (no navigation or dialog side effect).
- [x] 6.4 `npm run typecheck` and `npm run test` pass. Clean typecheck; 141/141 tests pass.
- [x] 6.5 `openspec validate --strict` for this change passes. Confirmed.
