import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.easyphotoprint.app',
  appName: 'Easy Photo Print',
  webDir: 'out/mobile',
};

export default config;
