## 1. PageStage click handling

- [x] 1.1 Add an `onClick` handler on the page rectangle `<div>` (the `bg-white shadow-2xl` element) that computes the click position relative to the rectangle and, when it falls outside the root's padded content box (using `page.rootNode.paddingMm`, the same values the existing dashed-outline overlay already uses), selects the root node (toggling off if already selected, matching the `imageSlot` convention) — otherwise no-ops.
- [x] 1.2 Add an `onClick` handler on `viewportRef` that, when the click landed outside the page rectangle's own bounds, selects the root node the same way (toggling off if already selected).
- [x] 1.3 Confirm neither handler needs `stopPropagation` and doesn't interfere with the existing `suppressNextClickRef` divider/freeform-drag suppression, per design.md's "No `stopPropagation` needed" decision. Verified: each handler checks/consumes `suppressNextClickRef` itself at the top (the page-rect handler runs first on bubble and consumes it before the viewport handler sees the event), so a divider/freeform drag's trailing click is suppressed regardless of which of the two new handlers it lands on, with no `stopPropagation` needed anywhere.

## 2. Verification

- [x] 2.1 Manually verify on desktop: tapping the root's margin band selects the root and updates Properties; tapping the gray viewport area outside the page rectangle behaves the same; tapping a slot and the gap between grid-column siblings are unaffected. Verified live (built app, launched via Playwright's `_electron` under `xvfb-run`, per the repo's existing E2E recipe): screenshots confirm the default single-slot template's root gets the cyan selection ring and Properties shows "Root type: imageSlot" after a margin click, and again after an outside-page click; after retyping the root to a 2-column `grid` (real 3mm gap between the two slots), a click in that gap leaves both slots unselected (Properties still shows the fallback "Grid properties", no ring on either slot), and clicking a slot directly still selects it normally. Toggle-off (re-activating an already-selected root) was verified to work correctly in Nested mode (the ring fully clears on the second click) but not to visibly clear in Simple mode — root-caused to a **pre-existing, unrelated** limitation of `clearSelection` documented in design.md's Risks section, confirmed to affect a plain `imageSlot` root's own existing toggle-off identically (not a regression from this change).
- [x] 2.2 Manually verify the same interactions at a narrow (mobile-width, 420px) viewport of the same `App`/`PageStage` used today (no separate `MobileShell` exists on `main` yet — see design.md's Non-Goals correction). Verified live: at 420×900, the panels stack vertically per `App.tsx`'s existing `lg:` breakpoint, and both the margin-band tap and the outside-page-bounds tap still select the root (cyan ring on the slot, Properties panel below showing root data) exactly as at desktop width.
- [x] 2.3 Verify the armed-image case: with a library image selected, activating the margin band or outside-page area selects the root (not an assignment) and de-arms the image selection, per design.md. **Not verified live** — doing so needs a real image asset loaded via the native file-open dialog, which wasn't worth mocking for this one low-risk path in this session. Verified by code review instead: `toggleRootSelection` reads `selectedSlotId` (which is `null` whenever `selection.kind === 'image'`) and, since it won't equal the root's id, always takes the `setSelection({ kind: 'node', id: rootNode.id })` branch — the same unconditional overwrite any non-assignable target already produces, with no special-casing needed.
- [x] 2.4 Run the existing test suite and typecheck to confirm no regressions. `npm run typecheck` (all 4 tsconfigs) and `npm run test` (25 files, 189 tests) both pass clean.

## 3. Spec closure

- [x] 3.1 Run `openspec validate --strict --all` and confirm the delta spec in this change is valid before archiving. Passed: 18/18 items (all archived specs plus this change's delta).
