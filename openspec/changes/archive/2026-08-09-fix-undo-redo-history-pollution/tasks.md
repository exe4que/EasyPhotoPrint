## 1. Root-cause fix

- [x] 1.1 In `src/store/index.ts`, add `equality: (pastState, currentState) => pastState.document === currentState.document` to the options object passed as the second argument to `temporal(...)`, alongside the existing `partialize`. **Found during this task**: `src/store/index.test.ts`'s `openProject` test used `setActiveTool('pan')` (a ui-only action) to populate `pastStates` before asserting `openProject` clears history — that setup was itself relying on the bug this change fixes. Changed to `addPage()`, a real document mutation.

## 2. Regression tests for the fix

- [x] 2.1 In `src/store/index.test.ts`, generalize the existing `describe('setViewMode', ...)` pattern (assert `temporal.getState().pastStates.length` doesn't grow) into new test cases covering `setSelectedElementIds`, `setActiveTool`, `setActivePageId` (to a different existing page), and `clearSelection` — each called on a fresh store, asserting zero `pastStates` growth. Renamed the describe block to `'UI-only actions do not pollute undo/redo history'`.
- [x] 2.2 Add a test asserting a UI-only action does not clear an existing `futureStates` (redo) entry: perform a real document change, undo it (populating `futureStates`), then perform a UI-only action (e.g. `setSelectedElementIds`), then assert `futureStates.length` is unchanged and `redo()` still re-applies the original document change. Implemented as a sequence of three ui-only actions (`setSelectedElementIds`, `setActiveTool`, `clearSelection`) after the undo, for slightly broader coverage than just one.
- [x] 2.3 Add a positive-case test confirming a real document-mutating action (e.g. `addPage`) still produces exactly one `pastStates` entry after the `equality` change — guards against the fix accidentally over-suppressing legitimate history entries.
- [x] 2.4 Run the full existing test suite and confirm every pre-existing "produces exactly one undo step" test (`addPage`, `removePage`, divider-drag, freeform-transform gestures, `setViewMode`) still passes unchanged. **Found during this task**: `openProject`'s test used `setActiveTool('pan')` — a ui-only action — to populate `pastStates` before asserting `openProject` clears history; that setup relied on the very bug being fixed, so it stopped populating history correctly under the fix. Fixed by switching that test's setup to `addPage()` (see task 1.1's note). 147/147 tests pass.

## 3. Simplify the now-redundant workarounds

- [x] 3.1 In `src/store/index.ts`, simplify `reanchorActivePageId`'s body to a plain `set({ ui: computeActivePageUi(state.ui, fallback.id, fallback) })` call, removing the `useEPPStore.temporal.getState().pause()`/`.resume()` wrap and its explanatory comment (now handled generally by `equality`). Verify with a targeted test: undo/redo sequences that trigger re-anchoring (see existing `index.test.ts` cases for `addPage`/`removePage` undo re-anchoring) still produce the correct `pastStates`/`futureStates` shape after the simplification. Confirmed via the existing `addPage`/`removePage` "undoing ... re-anchors activePageId" tests, unchanged and passing.
- [x] 3.2 In `src/store/index.ts`, remove the `setViewMode` override entirely (delete the whole overriding property from the returned store object and its comment), letting `createUiSlice`'s own plain-`set()` `setViewMode` — already spread in via `...createUiSlice(...)` — stand as the only implementation. Re-run the `print-preview` change's `setViewMode` non-pollution test (`src/store/index.test.ts`'s `describe('setViewMode', ...)`) to confirm it still passes without the override. Confirmed passing (describe block renamed in task 2.1 to `'UI-only actions do not pollute undo/redo history'`, the `setViewMode` test still first in it).
- [x] 3.3 Update the code comment above `reanchorActivePageId` (currently explains the pause/resume precaution) to instead reference the store-wide `equality` config as the mechanism that keeps UI-only re-anchoring out of tracked history.

## 4. Verification

- [x] 4.1 `npm run typecheck` and `npm run test` pass. Clean typecheck; 147/147 tests pass.
- [x] 4.2 Manually exercise the app (or a scripted Electron/Playwright pass, per this repo's established E2E verification recipe): select several elements, switch pages, toggle layout mode, then check that Undo has no effect until a real document change is made; make a document change, undo it, then select an element, and confirm Redo still re-applies the undone change. Verified end-to-end under `xvfb` with Playwright's `_electron`, driving the real `Edit > Undo`/`Edit > Redo` native menu items: added a page (real change), did several page-switch + selection actions (ui-only), then a single Undo correctly reverted exactly the page-add (not consumed by the ui-only actions), a second Undo was a no-op (no phantom entries left), Redo restored the page, and a further ui-only action after the redo didn't block a subsequent Undo.
- [x] 4.3 `openspec validate --strict` for this change passes. Confirmed.
