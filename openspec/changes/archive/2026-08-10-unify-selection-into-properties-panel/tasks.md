## 1. Unified selection state

- [x] 1.1 In `src/store/uiSlice.ts`, replace `selectedElementIds: string[]` with `selection: { kind: 'node'; id: string } | { kind: 'image'; id: string } | null` in `UiState` (export the `Selection` type). Default to `null` in `createInitialUiState()`.
- [x] 1.2 Replace `setSelectedElementIds`/`setSelectedElementIds` action with `setSelection: (selection: Selection) => void` (plain `set()`, same shape as the other `ui` setters) in `UiSlice`.
- [x] 1.3 Update `computeDefaultSelection` and `computeActivePageUi` to produce/consume `Selection` instead of `string[]` — Simple mode still defaults to `{ kind: 'node', id: rootId }`, Nested mode still defaults to `null` (unchanged logic, new shape).
- [x] 1.4 Update `clearSelection` to set `selection` to `computeDefaultSelection(...)`'s result under the new shape (same Simple-mode-falls-back-to-root / Nested-mode-empties behavior as today).

## 2. Migrate existing selection call sites

- [x] 2.1 In `src/components/canvas/PageStage.tsx`, replace every `setSelectedElementIds([id])` with `setSelection({ kind: 'node', id })` and every `selectedElementIds[0] ?? null` read with the equivalent derived from `selection` (`selection?.kind === 'node' ? selection.id : null`).
- [x] 2.2 In `src/components/canvas/FreeformElement.tsx`, update the `onSelect` callback wiring in `PageStage.tsx` (where `FreeformElementView`'s `onSelect={() => setSelectedElementIds([element.imageNodeId])}` is set) to use `setSelection({ kind: 'node', id: element.imageNodeId })`.
- [x] 2.3 In `src/components/panels/LayoutTreePanel.tsx`, migrate `setSelectedElementIds`/`selectedElementIds[0] ?? null` the same way (its own per-row type `<select>` and `retypeLayoutNode` usage are unrelated and stay as-is).
- [x] 2.4 In `src/components/panels/ImageLibraryPanel.tsx`, remove the `selectedImageAssetId`/`onSelectImageAssetId` props; read `selection?.kind === 'image' ? selection.id : null` and call `setSelection({ kind: 'image', id: asset.id })` (or `setSelection(null)` to toggle off) directly from the store instead.
- [x] 2.5 In `src/App.tsx`, remove the local `selectedImageAssetId` `useState` and its threading into `ImageLibraryPanel`/`SelectionPanel` — `ImageLibraryPanel` no longer needs those props (task 2.4); `SelectionPanel` is removed (task 3).

## 3. Retire SelectionPanel, absorb into PropertiesPanel

- [x] 3.1 In `src/components/panels/PropertiesPanel.tsx`, resolve the selected node from `selection?.kind === 'node' ? selection.id : null` (replacing the `selectedElementIds[0] ?? null` read), and change the `contextNode` fallback to unconditional `selectedNode ?? activePage.rootNode` — dropping the `layoutMode === 'simple' ? ... : rootNode.type === 'grid' ? ... : null` branching and the `!contextNode` "select a node" placeholder view entirely.
- [x] 3.2 Add a new early-return branch in `PropertiesPanel` for `selection?.kind === 'image'`: render that image asset's filename and pixel dimensions (migrated from `SelectionPanel`'s "Selected library image" block), plus a way to clear the selection (migrated "Clear selected image" button, now calling `setSelection(null)`).
- [x] 3.3 Delete `src/components/panels/SelectionPanel.tsx`.
- [x] 3.4 In `src/App.tsx`, remove the `SelectionPanel` import and its JSX usage.

## 4. Generalize the type-changing control

- [x] 4.1 In `src/components/panels/PropertiesPanel.tsx`, move the type-changing `<select>` (currently `rootTypeSelector`, gated to `layoutMode === 'simple' && contextNode?.id === activePage.rootNode.id`) out of that conditional so it always renders for whatever `contextNode` currently is, and change its `onChange` to call `retypeLayoutNode(activePage.id, contextNode.id, nextType)` instead of `setSimpleRootType`. Renamed to `nodeTypeSelector`, label reads "Root type" vs. "Node type" depending on whether `contextNode` is the page root.
- [x] 4.2 In `src/store/documentSlice.ts`, remove `setSimpleRootType` (action + its `DocumentSlice`/`EPPStore` type entry) and its now-unused helpers `setSimpleRootTypeForPage` and `firstAssignedImageAssetId`. Verify `normalizeRootForSimpleMode` and `normalizePageForSimpleMode` are left untouched (grep confirms `normalizeRootForSimpleMode` has a second caller, `normalizePageForSimpleMode`, independent of the removed code). Confirmed `cloneAsSimpleImageSlot`/`normalizeGridConfig` still have other live callers, not dead.
- [x] 4.3 In `src/store/documentSlice.test.ts`, replace every `state.setSimpleRootType('page-1', <type>)` call used purely as test setup (the freeform-element tests) with the equivalent `state.retypeLayoutNode('page-1', 'root-grid', <type>)`. For the test specifically asserting `setSimpleRootType`'s own clearing behavior ("switches the Simple root type to freeformCanvas and clears slot assignments"), rewrite it to assert the same outcome (`rootNode.type`, `freeformElements === []`, `assignments === {}`) through `retypeLayoutNode` instead — confirm `retypeLayoutNode`'s `filterAssignmentsForRootNode` pruning still clears the prior assignment (it should, since the retyped node's id is no longer a tracked `imageSlot` id afterward), don't assume without checking the test passes. Confirmed: all 32 tests in the file pass, including the renamed "retypes the root to freeformCanvas and clears slot assignments".

## 5. Tests

- [x] 5.1 Update `src/store/uiSlice.test.ts` for the new `Selection` shape: default `null`, `setSelection` toggling between `{kind:'node',...}`/`{kind:'image',...}`/`null`, `clearSelection`'s Simple/Nested fallback behavior, `computeActivePageUi`'s selection-on-page-switch behavior — same test intents as today, updated to the new field. Also updated `src/store/index.test.ts`'s ui-only-actions-don't-pollute-history tests (`setSelectedElementIds` → `setSelection`) and the `startNewProject` initial-`ui`-shape snapshot.
- [x] 5.2 Confirm (or add, if missing) a store-level test that selecting an image while a node is selected actually clears the node selection and vice versa (the core "app-wide single selection" guarantee) — this is now representable as a single-field transition, verify it's covered. Added to `uiSlice.test.ts`.

## 6. Verification

- [x] 6.1 `npm run typecheck` and `npm run test` pass. Clean typecheck; 148/148 tests pass.
- [x] 6.2 Manually exercise the app (or a scripted Electron/Playwright pass, per this repo's established E2E verification recipe): select a slot on the canvas (Properties shows slot props), select a library image (Properties shows image details, no more Selection panel), select a container node in Nested mode (Properties shows its structural controls), clear selection in both Simple and Nested mode with every root node type (Properties always shows the root's properties, never a placeholder), and change a node's type from Properties for both a root and a non-root node. Verified end-to-end under `xvfb` with Playwright's `_electron` across two passes: (1) no "Selection" panel exists anywhere; default (nothing selected) shows Slot properties for the imageSlot root; selecting a library image shows "Selected image" with filename/dimensions and the slot-properties view disappears; selecting a node brings back node properties and clears the image selection; retyping the root from Properties (imageSlot → grid) shows Grid properties. (2) Retyped root to `horizontal`, switched to Nested mode — a non-grid root still shows Container properties, no placeholder; selected a non-root child node from the layout tree, confirmed the type-selector label reads "Node type" (not "Root type"), retyped it to `grid`, confirmed Grid properties appears. Also fixed a stale UI-copy reference in `ImageLibraryPanel.tsx` ("...in the Selection panel" → "...in the Properties panel") found during this pass.
- [x] 6.3 `openspec validate --strict` for this change passes. Confirmed.
